<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_light_purple" /> <!-- Color de fondo del drawable -->
    <corners android:radius="16dp" /> <!-- Radio de las esquinas redondeadas -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" /> <!-- Padding interno -->
    <stroke
        android:width="4dp"
        android:color="@color/pastel_yellow" /> <!-- Borde del drawable -->
</shape>
